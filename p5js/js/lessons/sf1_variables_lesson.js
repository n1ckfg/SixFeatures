// 1. Variable: a place in memory that can store a piece of data.

function setup() {
    createCanvas(200, 200);

    let x = 100;
    let y = 100;
    let s = 50;

    background(127);
    ellipse(x, y, s, s);    
}

